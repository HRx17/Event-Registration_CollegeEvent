<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.updates2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logofinal_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Updates2021"
        android:usesCleartextTraffic="true">
        <activity android:name=".MainActivity3" />
        <activity android:name=".singleregister" />
        <activity
            android:name=".log_in"
            android:noHistory="true"
            android:finishOnTaskLaunch="true" />
        <activity
            android:name=".sign_in"
            android:finishOnTaskLaunch="true" />
        <activity
            android:name=".MainActivity2"
            android:finishOnTaskLaunch="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".teamRegister" />
        <activity android:name=".teamof2" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>